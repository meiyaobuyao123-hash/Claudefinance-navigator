# 模块开发分工指南

> 用于**多窗口并行开发**时的模块划分。
> 每个 Claude 会话专注一个模块，避免冲突。

---

## 模块锁定状态

> 开始开发某模块前，在此处标记 🔒 防止冲突

| 模块 | 当前负责会话 | 状态 | 最后活跃 |
|------|------------|------|---------|
| AI 诊断模块 | 主会话 | 🟡 进行中 | 2026-02-27 |
| 产品导航模块 | 未分配 | 🔴 待开发 | - |
| 工具箱模块 | 未分配 | 🔴 待开发 | - |
| 知识库模块 | 未分配 | 🔴 待开发 | - |
| 用户中心模块 | 未分配 | 🔴 待开发 | - |
| 基础架构 | 主会话 | ✅ 稳定 | 2026-02-27 |

---

## 模块一：AI 诊断模块

**Git 分支**：`dev`（当前直接在 dev 开发，后续可建 `feature/ai-chat`）

**文件范围**：
```
lib/features/ai_chat/
├── presentation/
│   └── pages/ai_chat_page.dart    ← 主文件（已开发）
└── (待扩展)
lib/core/config/api_keys.dart      ← API Key（已配置）
lib/core/constants/app_constants.dart  ← API URL
```

**已完成**：
- [x] Claude API 接入
- [x] 多轮对话 UI
- [x] 气泡消息组件
- [x] 打字动画指示器
- [x] 快捷回复建议
- [x] AI 角色"明理"系统提示词

**待开发**：
- [ ] `flutter_markdown` 渲染 AI 回复（**优先**）
- [ ] 流式输出（Streaming API）
- [ ] 对话历史本地持久化（Hive）
- [ ] AI 推荐结果解析 → 产品卡片跳转

**进入此模块的新会话提示**：
```
请读取 .claude/PROJECT.md 和 .claude/MODULES.md
我要继续开发 AI 诊断模块（lib/features/ai_chat/）
当前状态：基础对话可用，需要添加 Markdown 渲染
```

---

## 模块二：产品导航模块

**Git 分支**：建议 `feature/navigation`

**文件范围**：
```
lib/features/navigation/
├── data/
│   └── products_data.dart    ← 产品数据（静态）
├── domain/
│   └── product_model.dart    ← 产品数据模型
└── presentation/
    └── pages/
        ├── navigation_page.dart       ← Tab 入口
        ├── mainland_products_page.dart
        ├── hk_products_page.dart
        └── crypto_page.dart
```

**设计规格**：
- 三大板块：🇨🇳 大陆 / 🇭🇰 香港 / 🔐 加密
- 产品卡片字段：名称、风险等级（⭐）、收益区间、起投门槛、流动性、平台跳转按钮
- 每个产品支持展开查看详情（底部弹窗或详情页）
- 平台跳转：`url_launcher` 打开对应 App / 网页

**产品数据结构**：
```dart
class ProductModel {
  final String id;
  final String name;           // 货币基金
  final String category;       // 现金管理
  final String region;         // mainland / hongkong / crypto
  final int riskLevel;         // 1-5
  final String yieldRange;     // "1.3%-2.0%"
  final String minInvestment;  // "1元"
  final String liquidity;      // "T+0"
  final List<PlatformLink> platforms;
}
```

**进入此模块的新会话提示**：
```
请读取 .claude/PROJECT.md 和 .claude/MODULES.md
我要开发产品导航模块（lib/features/navigation/）
这是全新模块，从零开始开发
参考 .claude/PROJECT.md 中的"内容范围"作为数据来源
```

---

## 模块三：工具箱模块

**Git 分支**：建议 `feature/tools`

**文件范围**：
```
lib/features/tools/
├── presentation/
│   └── pages/
│       ├── tools_page.dart            ← 工具列表（已有占位符）
│       ├── compound_calculator_page.dart
│       ├── goal_calculator_page.dart
│       └── inflation_calculator_page.dart
└── logic/
    └── calculators.dart               ← 计算逻辑
```

**三个计算器规格**：

| 工具 | 输入 | 输出 |
|------|------|------|
| 复利计算器 | 本金 + 年利率 + 年限 | 终值 + 利息 + 折线图 |
| 目标倒推 | 目标金额 + 年限 + 预期利率 | 每月需存金额 |
| 通胀侵蚀 | 本金 + 通胀率 + 年限 | 购买力折损 + 对比图 |

**进入此模块的新会话提示**：
```
请读取 .claude/PROJECT.md 和 .claude/MODULES.md
我要开发工具箱模块（lib/features/tools/）
先做复利计算器：本金+年利率+年限 → 终值，需要折线图展示（fl_chart 库）
```

---

## 模块四：知识库模块

**Git 分支**：建议 `feature/learn`

**文件范围**：
```
lib/features/learn/
├── data/
│   └── articles_data.dart    ← 文章静态数据
└── presentation/
    └── pages/
        ├── learn_page.dart
        └── article_detail_page.dart
```

**内容规划**：
- 产品百科（每类产品一篇科普文）
- 风险教育（如何看风险等级）
- 内地 vs 香港对比
- 搜索 + 分类筛选

**进入此模块的新会话提示**：
```
请读取 .claude/PROJECT.md 和 .claude/MODULES.md
我要开发知识库模块（lib/features/learn/）
先做产品百科列表页和文章详情页
```

---

## 模块五：用户中心（需后端）

**状态**：等待后端搭建后开发

**Git 分支**：建议 `feature/auth`

**依赖**：Node.js/FastAPI 后端 + PostgreSQL + JWT

**进入此模块的新会话提示**：
```
请读取 .claude/PROJECT.md 和 .claude/MODULES.md
我要搭建后端并开发用户登录注册功能
后端技术栈：Node.js + Express + PostgreSQL + JWT
```
